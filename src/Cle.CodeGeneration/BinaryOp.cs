namespace Cle.CodeGeneration
{
    /// <summary>
    /// Arithmetic and logical binary operations.
    /// Division and remainder are handled separately.
    /// </summary>
    internal enum BinaryOp
    {
        Invalid,
        Add,
        Subtract,
        Multiply,
    }
}
